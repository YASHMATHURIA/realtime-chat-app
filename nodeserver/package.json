{
  "name": "nodeserver",
  "version": "1.0.0",
  "description": "this is a node server",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "yash",
  "license": "ISC",
  "devDependencies": {
    "nodemon": "^2.0.20"
  },
  "dependencies": {
    "socket.io": "^2.3.0"
  }
}
